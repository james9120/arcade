name: Apply Web Swing Weight Pass
on:
  push:
    branches: [fix/web-swing-weight]
    paths: [games/web-swing/weight_patch.py, .github/workflows/web-swing-weight.yml]
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/web-swing-weight
      - run: python3 games/web-swing/weight_patch.py
      - run: rm games/web-swing/weight_patch.py .github/workflows/web-swing-weight.yml
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Give Web Swing grounded pendulum physics"
          git push origin HEAD:fix/web-swing-weight
