# Arcade

A pile of tiny HTML games we can play in a phone browser.

**Play:** https://james9120.github.io/arcade/

Each game lives in `games/<name>/` with its own `index.html`. The homepage lists them.
